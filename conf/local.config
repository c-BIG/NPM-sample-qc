/*
 *  Nextflow config file for local profile
 */

executor {
   name = 'local'
}

singularity {
    enabled = true
    autoMounts = true
    cacheDir = '/home/ubuntu/NPM-sample-qc/containers'
}

process {
    container = singularity.cacheDir + '/' + params.container
}
