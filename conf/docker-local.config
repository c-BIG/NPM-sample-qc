/*
 *  Nextflow config file for local profile
 */

executor {
   name = 'local'
}

docker {
    enabled = true
    autoMounts = true
    cacheDir = '/home/ubuntu/NPM-sample-qc/containers'
}

process {
    container = docker.cacheDir + '/' + params.container
}
